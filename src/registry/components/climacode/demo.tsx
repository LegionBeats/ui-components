import Climacode from "./climacode";

export default function ClimacodeDemo() {
  return (
    <div className="w-full max-w-3xl mx-auto rounded-lg overflow-hidden border bg-black">
      <Climacode height={520} />
    </div>
  );
}